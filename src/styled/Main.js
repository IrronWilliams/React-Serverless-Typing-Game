//sets background color. The height of the entire page. 100vh makes sure the height is always as tall as the 
//window itself. Background color is like an off white.

import styled from 'styled-components'

export const Main = styled.div`
    margin: 0;
    background-color: var(--main-bg-color);
    min-height: 100vh;
    
`